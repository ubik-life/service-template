# Architecture

## Схема иерархий модулей

<!-- Дерево модулей от верхнего уровня к нижнему. Показывает кто кого вызывает. -->
<!-- Правила: каждый узел — одна ответственность, стрелки только сверху вниз, I/O отделены от бизнес-логики -->

```
main
├── server (HTTP-сервер, роутинг)
│   ├── handlers/
│   └── middleware/
├── store (работа с БД)
└── ...
```

## Блок-схема алгоритма головного модуля

<!-- Управляющий поток main() при запуске программы. -->
<!-- Показываем только управляющий поток, каждая ветка ошибки — явно. -->

```
main()
  │
  ├─ Загрузить конфигурацию из ENV
  │  └─ Нет обязательных переменных? → panic с явным сообщением
  │
  ├─ Открыть БД, запустить миграции
  │  └─ Ошибка? → panic
  │
  ├─ Инициализировать зависимости
  │
  ├─ Собрать роутер
  │  └─ /v1/... → handlers
  │
  └─ Запустить HTTP-сервер
     └─ Graceful shutdown по SIGTERM
```
